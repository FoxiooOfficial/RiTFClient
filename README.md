# RiTF (under construction)

RiTF is divided into:
- Client: Android application for old devices (API 8+) that sends HTTP GET request to Host to get data.
- Host: A Python script that runs a server that allows the client to query the endpoint to return photos/videos with the given tags.